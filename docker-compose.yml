services:
  ml-server:
    image: dhruv2k3/biasbreaker-ml-server:latest
    container_name: ml-server
    ports:
      - "8001:8001"
    env_file:
      - .env
    networks:
      - biasbreaker-net
networks:
  biasbreaker-net:
    driver: bridge